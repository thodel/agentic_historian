# SSRQ Corpus Project

## Overview
This project contains analysis and tooling for the **SSRQ** corpus TTL file located at:
```
/home/dh/resources/ssrq__fuseki_042810.ttl
```
The TTL file is ~71 MiB and contains **27 996** person entities (`pers:person`) and **9 134** organization entities (`pers:org`).

## Contents
- `README.md` – This overview.
- `analyze.sh` – Simple shell script that extracts basic statistics (entity counts, sample entries).
- `sample/` – A few extracted example snippets for quick inspection.

## Quick Stats
- **File size:** 72 MiB
- **Lines:** 1 300 938
- **Persons:** 27 996
- **Organizations:** 9 134

These numbers were obtained with `grep -c "^<http://ssrq-sds-fds.ch/Register/#per"` and `grep -c "^<http://ssrq-sds-fds.ch/Register/#org"`.

## Next Steps
- Build the SSRQ MCP server (`ssrq_mcp`) to expose a REST API.
- Integrate with OpenClaw for cross‑corpus queries with KF and EOS.
- Add more sophisticated analyses (e.g., name index, linking HLS identifiers).

---
*Generated by OpenClaw on $(date)*