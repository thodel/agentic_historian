# 2026-07-09

## agentic_historian PR review — 1:00 AM

### Epic #95 PR status (PRs #114–#127)
All 14 PRs are closed:
- **#114–#117** (feat/ah-96,97,98,99): closed (already pushed by previous sessions)
- **#118** (fix/ah-100-small-correctness-trio): CLOSED
- **#119–#121** (fix/ah-101,102,103): MERGED
- **#122** (fix/ah-104-minimal-ci): MERGED
- **#123** (fix/ah-105-auth-and-path-traversal): MERGED
- **#124** (fix/ah-112-error-log-persistence): MERGED
- **#125** (fix/ah-111-cer-honesty-stopwords): MERGED
- **#126** (fix/ah-113-repo-hygiene): MERGED
- **#127** (feat/kh-mcp-methodology): MERGED
- **#128** (feat/atr-gateway-client): CLOSED

All epic #95 branches were already pushed/purged from remote. Nothing left behind.

### Local uncommitted work found & pushed
- **updater.py** (240 lines) + **test_ah_246_updater.py** (350 lines) — offline-testable extraction of update.sh logic
  - Branch: `fix/ah-246-updater-module`
  - Pushed + PR #252 created
  - Closes #246
  - Note: untracked `agentic_historian/data/feedback/` dir (routing.jsonl) — left as-is, not relevant to this PR

### Other open PRs on repo
- #128: closed
- All other open PRs (#192–#251) are non-draft and already merged or closed

### Local main was behind origin/main by 8 commits
- Did `git checkout main && git pull --ff-only` — now current

### Summary
- ✅ PR #252 created: feat(#246) updater module — pushed, ready for review
- ℹ️ Epic #95: fully wrapped up, no stale branches
- ℹ️ Local main now synced with origin/main
# 2026-07-09

## agentic_historian PR review — 1:00 AM
[previous content unchanged]

## #238 implementation progress (as of 18:06)

### Branch
- `fix/ah-238-orchestrator-gate2-publish` from `main` (commit `bf0aa86`)

### Task 1 — Orchestrator fusion wiring ✅
- `config.py`: `FUSION_AGREEMENT_CER_THRESHOLD = 0.05` added
- `orchestrator.py`: Phase 3 replaced 2-way `reconcile()` with `fusion.fuse()` when
  `ENABLE_MULTI_ENGINE_FUSION=True`; agreement gate skips LLM arbitration when
  max pairwise CER < 0.05; falls back to legacy 2-way reconcile when flag is False
- Stores `fusion_strategy`, `fusion_arbitrated`, `fusion_agreement_cer`, `fusion_llm_skipped`
  in `ctx.a_meta`

### Task 2 — Publisher recognitions ✅
- `utils/publish_github.py`:
  - `_index_md()`: added collapsible "## Recognition results" section parsing
    `pipeline.json` → `recognitions[]`, shows per-engine output in `<details>` blocks
    with provenance badges (⚖️ vote / 🤖 LLM-arbitrated), fused text provenance note
  - `publish_doc()`: generates `recognitions/<engine>.txt` files for each engine in
    `pipeline.json.recognitions`, plus `recognitions/fused.txt` with the fused text

### Task 3 — Gate-2 N candidates ✅
- `path_compare.py`:
  - `PATHS` removed (was hardcoded trio); `compare_paths()` now derives names from `paths` dict
  - `compute_disagreements()`: new — word-level alignment via difflib.SequenceMatcher,
    finds per-token disagreement spans across N engines
  - `render_compare_card()`: dynamic N-path display, per-span disagreement section
  - `apply_path_choice()`: validates against available paths (not fixed tuple),
    records per-span override in `state.gate_decisions["span_overrides"]`
  - `build_view()`: one `_PathButton` per available path (not fixed 3)
  - `LABELS` dict: maps known engine names to display labels

### Tests (20 total, 6 failing as of last run)
- `tests/test_ah_238_orchestrator_gate2_publish.py` — 20 tests
- Failing (need fix before commit):
  1. `compute_disagreements_two_engines` — algorithm returns 0 spans (bug in column building)
  2. `build_view_one_button_per_path` — discord.ui.View.__init__ needs event loop
  3. `test_agreement_gate_skips_llm_when_candidates_agree` — CER=6.2% > 5% threshold
  4. `test_fusion_flag_calls_fuse` — test logic too convoluted, mock not working
  5. `test_render_compare_card_disagreement_spans` — keyword mismatch in assertion
  6. `test_publish_doc_generates_recognition_files` — needs pipeline.json in TRANSCRIPTIONS_DIR too

### Next steps to complete #238
1. Fix `compute_disagreements()` algorithm (column-building bug — rewrite with simpler approach)
2. Fix `build_view` test (mock discord.ui.View.__init__)
3. Fix agreement gate test (use identical strings: CER=0 < 5%)
4. Simplify fusion test (remove complex mock orchestration)
5. Fix disagreement_spans keyword assertion
6. Fix publisher test (create pipeline.json in TRANSCRIPTIONS_DIR)
7. Run tests → all pass
8. Commit + push + PR (do NOT merge)