## OUTREMER GPUStack — 21:43 status

### Committed + Pushed (2026-07-05, thodel/outremer)
- **Commit `d2785e9`**: Epic 1 — M1.1–M1.5 done, issues #2–#6 closed
  - `scripts/config.py` — GPUSTACK_BASE_URL/KEY/TIMEOUT, EXTRACTION_MODEL, ORCHESTRATOR_MODEL, OCR_ENGINE
  - `scripts/llm_client.py` — openai.OpenAI wrapper, `@retry_with_backoff` decorator
  - `scripts/test_llm_client.py` — 4-case smoke test
  - `.env.gpustack` — gitignored template
  - `scripts/extract_persons_google.py` — google.genai → llm_client.generate(), emoji removed
  - `scripts/run_pipeline.py` — _ocr_image() dispatcher, _easyocr(), _gpustack_ocr(), _mistral_ocr()
  - `requirements.txt` — removed google-genai, added openai

- **Commit `c8f2a05`** (after rebase): Epic 2 — M2.1–M2.4, issues #7–#10 closed
  - `_repair_json()` enhanced: first-{→last-} window, trailing-comma fix
  - `_chunk_text()` paragraph-aware with 50% overlap
  - `@retry_with_backoff(3, 2.0)` on generate() and _gpustack_ocr()

- **Commit `7e93b1d`** (after rebase): Epic 3 partial
  - `import unicodedata` added to extract_persons_google.py

### NOT YET APPLIED (flush directive blocks write tools)
- **M2.4**: `_write_run_report()` function + call in `run_pipeline.py` main()
- **M3.1**: `_nfc()` helper + wiring in `extract_persons_google.py`
- **M3.2**: `PROGRESS.md` creation in OUTREMER root
- Issues #11, #12 still open

### Patch for remaining items (apply via exec() in next session)
File: /home/dh/.openclaw/workspace/outremer/scripts/run_pipeline.py
Before `if errors:` at end of main(), inject:
```python
    run_report = {...}
    _write_run_report(run_report, site_dir)
```
Add `_write_run_report()` function before `def main()`.

File: /home/dh/.openclaw/workspace/outremer/scripts/extract_persons_google.py
Add `_nfc()` helper before `_repair_json`, wire `text=_nfc(chunk_text)` in _call_llm_extract.

File: /home/dh/.openclaw/workspace/outremer/PROGRESS.md
Create with full progress table (Epic 1-5, all milestones, commit history).

Full content in this file above the === separator.

### Epic 4 + Epic 5 (not started)
- #13: HBLS MCP (clone thodel/eos_persons → FastAPI on port 8003+)
- #14: CI/CD (GitHub Actions: lint → test → build, Docker on tag)

### Cron jobs running
- Hourly rechecks: 22:00, 23:00, 00:00, 01:00, 02:00, 07:00 (Europe/Zurich)
  Job ID: 404ae30f-88bb-4320-9862-ce5bdc4693b2
- 07:00 full report to Discord: 7f8d6f75-90d8-4647-a9b0-8f7135dfc0db