# Environment
.env
.env*
.env.gpustack

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtualenvs — never commit these (a venv was once committed by accident)
.venv/
venv/
env/

# Hot-folder inputs / local source images
data/hot_folder/

# OpenClaw / Logs
*.log
openclaw-workspace-state.json

# Data (local) — regenerated at runtime, never commit.
# (gitignore has no inline comments — keep these on their own lines)
data/hot_folder/processed/
data/transcriptions/
data/descriptions/
data/outputs/
# RunState persistence (#225) and the routing-prior feedback log
# (its .jsonl slips past the *.json rule below):
data/runs/
data/feedback/
*.json

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# IDE
.idea/
.vscode/